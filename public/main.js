var update = document.getElementByClassName('wins-btn')

update.addEventListener("click", function() {
	fetch("something", {
		method: "put",
		headers: {"Content-Type": "application/json"},
		body.JSON.
	})
})